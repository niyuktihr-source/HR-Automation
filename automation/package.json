{
  "name": "automation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node src/dev.js",
    "auth": "node src/auth.js",
    "add-employee": "node src/addEmployee.js",
    "list-employees": "node src/listEmployees.js",
    "remove-employee": "node src/removeEmployee.js",
    "stop-employee": "node src/stopEmployee.js",
    "reset-employee": "node src/resetEmployee.js",
    "view-state": "node src/viewState.js",
    "mark-task": "node src/markTask.js",
    "test-emails": "node src/testEmails.js",
    "health": "node -e \"require('http').get('http://localhost:' + (process.env.WEBHOOK_PORT || 3000) + '/health', r => { let d=''; r.on('data', c => d+=c); r.on('end', () => console.log(JSON.parse(d))); }).on('error', e => { console.error('Engine not running:', e.message); process.exit(1); })\"",
    "verify-audit": "node -e \"require('dotenv').config(); const {verifyTodayLog} = require('./src/secureAuditLog'); const r = verifyTodayLog(); console.log(r); process.exit(r.ok ? 0 : 1);\"",
    "backfill-sheets": "node src/backfillInfoSheets.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "axios": "^1.17.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "googleapis": "^173.0.0",
    "node-cron": "^4.2.1",
    "nodemailer": "^8.0.10"
  }
}
